@page
@model MagazinMobilaOnline.Pages.Clienti.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].ClientNume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].ClientAdresa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].ClientNrTelefon)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Client) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClientNume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClientAdresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClientNrTelefon)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ClientID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ClientID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ClientID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
